# ~/.tmux.confの内容
# prefixをctrl + Spaceにする
set -g prefix C-t
# vimのキーバインドでペインを移動する
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# windowの分割
bind L split-window -h
bind J split-window -v

# ステータスバーの色を設定する
set -g status-fg white
set -g status-bg black

